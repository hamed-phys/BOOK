# استكشاف الأخطاء وإصلاحها

| العرض | السبب المرجح | الإصلاح الدقيق |
|---|---|---|
| الصفحة تقول إن قائمة الكتب لم تُحمّل | `books.json` غير صالح أو غير موجود | GitHub → `data/books.json` → Edit. افحص الأقواس والفواصل ثم افتح `tools/update-helper.html` للتحقق. |
| خطأ JSON بعد إضافة سجل | فاصلة مفقودة/زائدة أو اقتباس غير صحيح | بين سجلين استخدم `}, {`، ولا تضع فاصلة بعد آخر سجل. استخدم `"` للنصوص فقط. |
| الأداة تقول Duplicate ID | معرف كتاب أو تحديث مكرر | ابحث في الملف عن نفس `id`. غيّر معرف السجل الجديد فقط؛ لا تغيّر معرف كتاب منشور قديمًا. |
| تحديث مرفوض بسبب `book_id` | التحديث يشير إلى كتاب غير موجود/غير صالح | اجعل `book_id` مطابقًا حرفيًا لـ `id` موجود في `books.json`. |
| الحالة لا تظهر | خطأ إملائي في `status` | استخدم بالضبط: `review`, `translation`, `correction`, `final_review`, `complete`, `paused`, `coming_soon`. |
| التاريخ مرفوض | ليس تاريخًا حقيقيًا أو ليس `YYYY-MM-DD` | مثال صالح: `2026-09-11`. `2026-02-30` غير صالح. |
| النسبة مرفوضة | كتبت نسبة كنص أو خارج 0–100 | استخدم رقمًا مثل `65` أو `0` أو `100`، أو `null` إذا كانت مجهولة. لا تستخدم `"65%"`. |
| الغلاف لا يظهر | اسم الملف/الحالة الكبيرة مختلفة أو المسار خطأ | تحقق من `assets/covers/name.webp`. GitHub حساس لحالة الأحرف: `Cover.webp` مختلف عن `cover.webp`. |
| ظهر الغلاف العام بدل الصورة | الصورة فشلت في التحميل أو `cover` غير صالح | صحح الملف والمسار. الموقع يستبدل الصورة الفاشلة مرة واحدة فقط بالغلاف العام لتجنب حلقة أخطاء. |
| رفعت مجلدًا وظهر الموقع داخل مجلد إضافي | تم رفع المجلد الأب بدل محتوياته | يجب أن يكون `index.html` في جذر المستودع. فك ZIP ثم ارفع **المحتويات** مع المجلدات الداخلية. |
| يعمل على `USERNAME.github.io` ولا يعمل تحت `/book-review/` | مسار مكسور يبدأ بـ `/` أو رابط داخلي مكتوب كجذر مطلق | لا تستخدم `/assets/...` أو `/data/...`. استخدم البنية المرفقة؛ الوحدة المشتركة تستخرج base من `assets/js/common.js`. |
| فتح `index.html` محليًا يظهر أخطاء | فتحته بـ `file://` | هذا غير مدعوم. استخدم GitHub Pages أو خادم HTTP مؤقت. |
| زر القراءة غير نشط | الرابط فارغ/غير صالح/ليس HTTPS على `drive.google.com` | انسخ رابط المشاركة الحقيقي من Drive. `YOUR_GOOGLE_DRIVE_LINK_HERE` يعامل كغير متاح. |
| Drive يفتح عندي فقط | صلاحيات المشاركة ليست مناسبة | Share → General access. اختبر من حساب قارئ منفصل. لا تعتمد على وصول المالك. |
| المراجع لا يستطيع التعليق | دوره Viewer أو سياسات الحساب تمنع Commenter | راجع Share والدور المتاح. قد تحتاج تسجيل دخول أو إعدادات مؤسسة مختلفة. راجع `GOOGLE_DRIVE_SETUP.md`. |
| Pages يعرض 404 للموقع كله | مصدر النشر خطأ | Settings → Pages → Source: Deploy from a branch → Branch: `main` → Folder: `/(root)` → Save. ثم افتح الرابط الذي يعرضه GitHub. |
| `book.html?id=...` يقول «الكتاب غير موجود» | هذا ليس Hosting 404؛ المعرف مفقود/غير معروف/معطل | تحقق من قيمة `id` في الرابط ومن `enabled: true` ومن عدم تكرار المعرف. |
| رابط GitHub نفسه يعرض 404 قبل الموقع | المستودع/المسار/النشر غير صحيح | افحص اسم مستودع user site أو project site، ثم Pages URL الحقيقي في Settings → Pages. |
| الموقع لا يتغير بعد Commit | النشر لم يكتمل أو المتصفح يعرض نسخة مخزنة | انتظر انتهاء Pages deployment ثم حدّث الصفحة تحديثًا قويًا/امسح cache للصفحة. لا تضف cache-busting عشوائي إلى الملفات. |
| Deployment فشل | بنية المصدر أو إعداد Pages غير صحيح | راجع Settings → Pages، ثم تبويب Actions لتفاصيل تشغيل Pages الداخلي. تأكد أن `index.html` و`.nojekyll` في الجذر. |
| عناصر تخرج خارج الشاشة على الهاتف | نص/صورة/تعديل CSS طويل أو عنصر جديد بعرض ثابت | ارجع للتصميم الافتراضي، استخدم CSS logical properties و`min-width: 0`، وتجنب عروض px ثابتة للعناصر الكبيرة. اختبر 320 و375 px وZoom 200%. |
| لا تظهر تحديثات لكن الكتب تعمل | فشل `updates.json` | الموقع يتعمد إبقاء الكتالوج عاملًا ويعرض رسالة أن السجل غير متاح. أصلح JSON بدل اعتبار التاريخ صفرًا. |
| أداة الصيانة لا تحمل البيانات | تعمل عبر `file://` أو يوجد فشل في الملفات المنشورة | افتحها من GitHub Pages عبر HTTP(S)، أو استورد `books.json` و`updates.json` المحليين **معًا**. |

## إذا نسيت `.nojekyll`

في GitHub أنشئ ملفًا جديدًا في الجذر باسم `.nojekyll` واتركه فارغًا ثم Commit. الملف موجود أصلًا داخل ZIP.

## إذا احتجت الرجوع إلى نسخة سليمة

افتح الملف → **History** → افتح commit سليم → **Raw** → انسخ النص → ارجع للملف الحالي → **Edit** → استبدل هذا الملف فقط → **Commit changes**. لا ترجع المستودع كله للخلف بسبب خطأ في ملف واحد.
